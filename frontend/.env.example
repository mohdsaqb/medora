# Copy to .env and adjust. Never commit .env.
#
# Vite only exposes variables prefixed with VITE_, and they are read at build
# time, not at runtime.

# Backend API base URL, including the /api suffix.
VITE_API_URL=http://localhost:8185/api
